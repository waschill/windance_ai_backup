# Software upgrade continuation — 2026-09-25

William reported AL recovered and authorized finishing the remaining upgrades. Vega executed the work directly. Status: core application upgrades verified; Mac administrator work and final Windows checks still in progress. Do not describe the entire stack as complete.

## Recovery and verified upgrades

- Fresh sanitized GitHub restore point: `76651ecfdb41f2c46146c190073f81b54faf0cab` (September 25, 14:27 Mountain).
- AL: booted kernel `7.0.0-34-generic`, six expected containers running, production/lab Open WebUI `0.11.4`, Portainer `2.45.1`, Docker `29.8.1`, Compose `5.5.1`, Valkey `9.1.2`. Both WebUI version APIs, Portainer status, Valkey PONG and real SearXNG search passed; no failed systemd services. Lab is LAN-bound at 192.168.36.20:3001, so localhost:3001 is not its health address. Eight Ubuntu phased updates remain deferred. See the September 24 AL record for the original outage and backups.
- SAM: 195 explicitly selected installed packages upgraded, with dependency additions and the publisher's pcmanfm-to-pcmanfm-pi replacement. Debian now reports `13.7`; Chromium `153.0.8010.52`; reboot verified on kernel `6.18.50+rpt-rpi-2712`. Schedule API, rendered-page HTTP response and actual kiosk browser version passed after reboot. `dpkg --audit` was clear. Only `raspi-firmware` and `rpi-eeprom` remain pending and were deliberately excluded; no firmware flashing or major OS migration was performed.
- HAL: Ollama `0.34.1` to `0.34.4`, running API and local Gemma inference passed. All ten upstream model manifests exactly match registry response-byte SHA256 values; all 25 installed manifests, including 15 custom Windance aliases, remain byte-identical. No model pulls, persona replacements or model deletions.
- HAL applications: Angry IP Scanner `3.10.0`, balenaEtcher `2.1.7`, FFmpeg `9.0.2`, LibreOffice `26.8.0.3`, Teams `26198.304.4946.9672`, VC++ x64/x86 `14.51.36247.0`, Telegram `7.2.9` installed successfully. PuTTY, UltraVNC and WSL initially failed or needed elevation; William authorized the UltraVNC retry, and a single elevated retry returned success for all three. Final inventory confirmed PuTTY 0.85.0.0, UltraVNC 1.8.3.0, and WSL 2.7.13. LibreOffice's Windows Installer receipt requested a deferred Windows reboot; HAL was not rebooted during the conversation. Edge upgraded to 154.0.4258.37 using the official Microsoft MSI after its native updater did not advance. Publisher SHA256 and valid Microsoft Authenticode signature were verified; the existing browser profile was preserved. Winget now reports no known-version upgrades. Its unknown-version Geekbench entry was checked directly: installed Geekbench 6.6.0 is newer than winget's 6.4.0 candidate, so no downgrade was attempted.
- HERALD: Hermes `0.21.2` to exact stable `v2026.9.24`, code `f97608f178d1ffeca59860195ab7da295f7c8e5f`, package `0.21.5`. Candidate built separately; 87 upstream plugin/auth tests and five existing response-guard tests passed. Web and TUI builds and core imports passed. Existing Python dependencies already satisfied the release; only the editable Hermes package changed. Production dependency check passed. Local SearXNG patch and untracked local files retained. All 50 captured profile/config/plugin hashes unchanged. Actual Herald transform_llm_output hook registered; dashboard protected Work API returns 401 unauthenticated; Harness database/health passed. Active service jobs were reloaded; dashboard and gateway run in user/501, while the service bridges/Harness use gui/501. The latter require explicit kickstart after bootstrap.
- SAL: upgraded ca-certificates `2026-09-25`, libuv `1.53.0`, nss `3.130`, simdutf `9.2.1_1`, retaining old kegs. Node-RED remains current `5.0.7`; production Node remains pinned `26.8.1` after the verified 26.9.0 launchd networking regression. Newer Node is a compatibility exception, not a completed runtime upgrade. HTTP service remained healthy. Cloudflared `2026.9.3` is now active in the restarted root daemon; its startup version and public Herald route were verified.

## Nightly checker repair

The prior nightly failure was AL being offline and the consequent backup timeout. A fresh post-recovery read-only audit now has zero check failures. Additionally, Hermes currency now compares GitHub's latest stable release with tags at installed HEAD rather than declaring the verified stable release outdated because development main has newer commits. An unattended Hermes update cannot jump onto development main: an exact stable-tag update needs supervised compatibility/customization/Warden/service verification. Homebrew skips pinned packages and retains old kegs; macOS inventory uses absolute softwareupdate paths. Seventeen regression tests and a real release lookup passed. The 1:30 AM schedule remains installed. The audit honestly lists deferred categories; zero check failures does not mean no updates remain.

## Administrator work and preserved exceptions

`~/finish-mac-updates-20260925.sh` is staged on both SAL and HERALD, shell-syntax checked. It first requires local sudo authentication, pauses Warden, selects only offered Tahoe `26.7-25G229` plus Safari `27.0`; SAL additionally selects Xcode Command Line Tools `27.0` and restarts `system/com.cloudflare.cloudflared`. It never selects macOS 27 and does not automatically reboot. Run SAL first, independently verify/reboot, then HERALD. From HAL, interactive `ssh -t SAL /bin/bash /Users/zuzu/finish-mac-updates-20260925.sh` permits password entry on HAL without a monitor on SAL; never send or capture that password in chat. A visible HAL terminal was opened for William. Do not repeat a running installer or assume staging means execution. Warden must be resumed after independent post-maintenance checks.

Warden was paused successfully for this work and then resumed after measured healthy SAL/HERALD checks with zero incidents. Its own Codex+Claude consensus policy remains unchanged. The administrator script paused it again for active Mac maintenance; it must remain paused until post-maintenance checks pass.

SyncThing remains excluded under William's current-turn authorization rule. Level 8, firmware/EEPROM, business-data writes and persona changes remain excluded. SAM has a pre-existing Certbot failure for rdesk.reflectsody.com: the certificate expired June 16 and its public HTTP validation route is failing. The schedule service is healthy; this certificate issue is not a package-upgrade regression. No router exposure or DNS change was made. SAL's automatic Kasa reboot rule remains as documented September 24; do not assume Warden controls it.

## Private recovery locations

- SAM: `/home/williamschilling/maintenance-recovery-20260925`, including stopped schedule archive, root-protected system/boot archive, package plan/inventory and installation log. Preserve newer live schedule data when restoring.
- HAL: `C:/Users/wasch/Documents/WindanceMaintenanceRecovery/20260925-HAL`, including original Ollama binaries, model manifests, LibreOffice user profile, publisher metadata and per-package installer receipts.
- HERALD: `/Users/herald/services/maintenance-recovery-20260925`, including full original Hermes checkout/runtime, separate candidate, consistent SQLite backups, protected-file hashes, installation/build/test logs, service restart list and original nightly checker. Never publish private runtime or database backups. Do not restore old databases over newer user activity blindly.
- SAL: September 24 recovery directory retains Node rollback and local configuration; dependency-upgrade receipt is `brew-deps-20260925.log`. September 25 Apple installer logs, when execution begins, are under `services/maintenance-recovery-20260925`.

Official sources: https://github.com/NousResearch/hermes-agent/releases/tag/v2026.9.24 ; https://github.com/ollama/ollama/releases/tag/v0.34.4 ; https://github.com/node-red/node-red/releases/tag/5.0.7 ; https://github.com/cloudflare/cloudflared/releases/tag/2026.9.3 ; authenticated Debian/Raspberry Pi package metadata and Apple's softwareupdate catalog; publisher URLs and SHA256-verified Windows installers are recorded in HAL's recovery directory.

## Live continuation at 2026-09-25 21:06 UTC

William authenticated on SAL. The Apple installer is actively downloading the selected Tahoe 26.7 patch; no reboot or completion is claimed. HERALD Apple installation has not started. HAL Hermes Desktop was discovered at the older v2026.9.7 source/build; a separate exact-v2026.9.24 candidate is building. The running app uses remote connection mode and has not been closed or replaced. Private user settings remain untouched. The authoritative Herald upgrade plan now supersedes yesterday's AL hold and prohibits duplicate Forge job replay.


## HAL desktop installed — 2026-09-25 21:10 UTC

HAL Hermes Desktop was separately built from exact stable source f97608f178d1ffeca59860195ab7da295f7c8e5f (v2026.9.24; desktop package 0.17.6). The workspace-scoped npm install omitted a required shared lucide-react dependency; a full root npm ci using the upstream lock resolved the build without changing source. Bundle validation passed, including native node-pty binaries and renderer payload. The upstream validation helper incorrectly required a branch string despite the production stamp writer/reader explicitly supporting null for detached commits; the local check permits null while independently requiring the exact clean stable SHA. No production application code was patched.

After William's app closed, Vega backed up the full user-data directory privately, staged and swapped only the desktop package, then relaunched. Five new Hermes processes were running and established TCP connections to HERALD:9120. The saved connection, connections, secure-token-storage and native-oauth-tokens files were hash-identical to the pre-change copies. No user secrets were printed. The previous app remains alongside the release as win-unpacked.before-20260925 and in the private recovery directory. This verifies startup and transport; no message was sent as a UI canary.

HAL's separate resident local Hermes gateway remains at package 0.21.1 and is not covered by the desktop replacement. Its long-running Python processes were not interrupted. An isolated Python 0.21.5 candidate has compatible dependencies and successful core imports (only hermes-agent and pillow-heif changed). Assess gateway ownership/activity before changing its runtime. HERALD's production backend is already verified 0.21.5. Do not claim every installed Hermes runtime is updated.

SAL Apple installer completed download and entered Safari/Command Line Tools installation. Tahoe reboot/activation and HERALD Mac patch remain pending. The existing follow-through heartbeat has been updated and reactivated; it must inspect live processes and avoid duplicate installers.

## HAL runtime resolved; Mac activation pending — 2026-09-25 21:15 UTC

This supersedes the HAL local-runtime exception above. William confirmed the desktop was closed and authorized proceeding. The old local gateway accepted its supported planned-stop marker and drained cleanly; no force termination was needed. A private Hermes home backup, including data/config/auth, was taken with the gateway stopped. Source fast-forwarded cleanly to stable f97608f178d1ffeca59860195ab7da295f7c8e5f; uv installed only the required Hermes 0.21.5 and pillow-heif changes. Candidate web and TUI bundles built successfully and were deployed. All 130 installed Python packages passed dependency compatibility checks, core imports passed, and the gateway restarted through its existing Windows startup implementation. Fresh runtime status at 21:14:50 UTC reported running PID 13400, zero active agents, code_version 0.21.5 and the exact expected SHA. Four existing protected root config/auth/persona files were hash-identical to the private backup. Five Hermes desktop processes remained running. HAL client and local runtime now match the verified stable release.

SAL softwareupdate completed Safari 27.0 and Command Line Tools 27.0; installation history independently confirms both. macOS remains 26.6.2. The initial install command downloaded Tahoe but did not activate it without restart. William's pasted Terminal output confirms the command finished and returned to the local zuzu shell prompt. He has been given the exact follow-on command: sudo /usr/sbin/softwareupdate --install 'macOS Tahoe 26.7-25G229' --restart --user zuzu. SSH sudo -n still requires a password; no authentication was captured or bypassed. Wait for execution/reboot, then verify SAL. Warden remains deliberately paused.

HERALD has an improved staged script at /Users/herald/finish-herald-mac-updates-20260925.sh, syntax checked; its single privileged installer command includes --restart to avoid the two-step activation encountered on SAL. It remains unexecuted. Run only after SAL returns healthy, with private interactive authentication, and verify services before resuming Warden. The script selects only Safari27 and Tahoe26.7; macOS27 remains excluded.

## SAL patch verified — 2026-09-25 21:21 UTC

William initiated the explicit Tahoe restart. SAL returned on macOS 26.7; its GUI session is zuzu. Node-RED restarted automatically after login; startup log at15:19 Mountain confirms Node-RED5.0.7, Node26.8.1, HTTP serving and Started flows. Local HTTP1880 returned200. Cloudflared is running after boot. Warden's fresh21:19:28UTC check reports paused=true, zero incidents; all SAL outbox/scheduler/connection and HERALD application checks pass. Warden stays paused until HERALD maintenance finishes.

HERALD is still26.5.2, with FileVault enabled and authenticated restart supported. Its installed fdesetup manual confirms authrestart -delayminutes -1 prepares the next restart without immediately rebooting. The staged HERALD script now requests that one-restart authentication privately before softwareupdate --restart; it does not disable FileVault or capture credentials. Zero active/dispatching staff tasks were observed before requesting launch. William was given an interactive SSH command to run from SAL, avoiding the need for a monitor on HERALD. Await actual execution and post-reboot verification; do not infer success from staging.
