Revision 3 follow-up in session 20260924_144117_b3af41. Read the actual revised bytes in warden-consensus-v3 and verify all 15 manifested files (13 source/config/fixture files plus COMPILE_RECEIPT.txt and TEST_OUTPUT.txt). Return APPROVE or REJECT with concrete remaining blockers. No production deployment has occurred. Warden remains paused/unloaded.

At your explicit request I ran syntax compilation and isolated SAL tests after correcting the refactor; compileall succeeded and all 41 tests passed. Full unedited test output is included. No deployment code or real recovery ran. I agree with your prior blockers and did not execute the rejected runtime on production.

A/B fixed: removed orphaned elif, compiles. Polling now owns supervise_reviews(): verifying expires after 300s with an immediate attention notice plus escalation; pending_review after 600s and reviewing after 900s hold with notice. This deliberately runs in polling, not the potentially broken review worker. New incident detection also queues a notice independently of model availability. Tests cover unverified-recovery and pending-review deadlines. A diagnostic result cannot post late attention after polling resolved the incident (CAS test).
F2: deployment-manifest.json is included and contains SHA values calculated from the exact reviewed source files plus the pinned existing Claude wrapper. activate_warden.py embeds the SHA of that manifest as a literal. The enclosing MANIFEST.sha256 hashes both it and the activation source. No circular derivation at deploy time; activation verifies the pre-reviewed bytes.
F3/F7/F8: remote fingerprints now includes actual private claude_review.py. Activation compares it too. Config is staged on BOTH hosts. Live wrapper shasum observed: 0d85a798a945d7cddbec085624be19a350819d02ae79757106f84b3442028eab, exactly the pinned value. SAL stage config and HERALD stage config both present. Production activation.json still exists both hosts.
F4: replaced positional domain split with explicit stable top-level field/block allowlist. launchctl-fixtures.json captures actual four production label output structures, with nested values redacted BEFORE writing (no secrets copied). Tests exercise all four layouts and prove volatile PID/runs/state/count changes do not alter fingerprints. Actual unredacted runtime definition is only hashed in memory and not shared. Canary must further prove the parser on live launchd.
F5/F9: canary MUST return actual parsed Codex and Claude verdicts and a stored Claude session receipt before any test execution. Added audit_claude_session against actual profile state.db: exact model anthropic/claude-opus-5, billing_provider openrouter, and only review_list/review_read tool results (or no tools) are accepted. Test verifies unexpected memory tool rejects. Read-only inspection today confirmed your actual session has those exact model/provider values, and profile memory_enabled=false/user_profile_enabled=false. Same-account trust caveat retained.
F6 fixed: save receipt before cleanup, catch/record cleanup failure and save again, report unsuccessful cleanup, explicit raises replace assert in canary.
F10 intentional: two combined review/diagnosis workflows per UTC day. Budget exhaustion still alerts without more model spend; it is not an approval. Healthy polling uses no model.
F11 accepted: concise bounded reviewer reasons may reach William. He requested the problem/proposal/reason via Warden, and packets contain only sanitized operational observations.
Assurance caveat accepted: tool-returned file digests and manifest were compared; reviewer display may sanitize content. Vega separately pins/checks actual deployment hashes. We will not call the model's displayed reading cryptographically byte-exact. Numeric IP literals were replaced with localhost in the relevant sources.

Requested authorization scope if you approve: run the real consensus_canary ONLY on the disposable registered sleep job. It must produce two actual approve decisions, a valid pinned Claude session/tool audit, a performed receipt, live canary health, and successful cleanup. Then deploy the exact manifested runtime files and fixed manifest on SAL/Herald while paused, execute the reviewed activate_warden.py, verify at least two scheduled health samples plus review-worker exit status and the mirror/auth boundary. If any condition fails, do not activate; fix/resubmit. No production outage injection, task replay, arbitrary patch, upgrade, reboot, business change, SyncThing or Level 8. Existing report schedule and routing stay. Code/config changes after approval require a new reviewed revision.
